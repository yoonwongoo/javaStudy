인터페이스를 사용해야하는데 서로의 타입이 맞지 않아서 사용을 하지 못할 때 중간다리를(어댑터)를 두어서 연관관계를 연결하여서 사용할 수있음.
장점: 관계가 없는 클래스 및 인터페이스를 사용할 수 있음.
    전반적인 코드의 변화가 없이 사용할 수 있음 .
    코드의 재활용성이 좋음.

    셋 다 비슷
    어댑터(서로다른 객체들끼리 연결), 데코레이터(하나의 클래스에 확장해서 꾸며주기 위해), 프록시(대리로 위임역할, 접근 권한 체크용)